<div class="trick_form">
  {{ form_start(formAdd, {'attr': {'class': 'form-horizontal'}}) }}


  {# Les erreurs générales du formulaire. #}
  {{ form_errors(formAdd) }}

   <div class="media">

        <hr class="divider">

        <div class="form-group">
          {{ form_label(formAdd.thumbnail, "Image à la une", {'label_attr': {'class': 'control-label'}}) }}
          {{ form_errors(formAdd.thumbnail) }}
          
          {{ form_widget(formAdd.thumbnail) }}
          <p class="file-return"></p>
        
        </div>

        <hr class="divider">

    
        <div class="form-group">
            {{ form_label(formAdd.images, "Images", {'label_attr': {'class': 'control-label'}}) }}
            {{ form_errors(formAdd.images) }}
 
            {{ form_widget(formAdd.images)  }}
             <p class="file-return"></p>

        </div>                    

        <div class="add"><a href="#" id="add_image" class="btn-form">Ajouter une image</a></div>

        <hr class="divider">


        <div class="form-group">
            {{ form_label(formAdd.videos, "Vidéos", {'label_attr': {'class': 'control-label'}}) }}
            {{ form_errors(formAdd.videos) }}
                
            {{ form_widget(formAdd.videos) }}

                       
        </div>
         <div class="add"><a href="#" id="add_video" class="btn-form">Ajouter une vidéo</a></div>
    </div>

   
        <hr class="divider">

  <div class="row trick_details">
              <div class="col-md-12 trick_information">
                <div>
                  <p>Date de création :<br />
                    {{'now'|date('d/m/Y à H:i')}}</p>
                </div>                  
              </div>
        </div>

 <div class="form-edit">
    {{ form_label(formAdd.name, "Nom du trick", {'label_attr': {'class': 'control-label'}}) }}
    {{ form_errors(formAdd.name) }}
    {{ form_widget(formAdd.name, {'attr': {'class': 'form-control name_input'}}) }}
  </div>

  <div class="form-edit">
    {{ form_label(formAdd.description, "Description de la figure", {'label_attr': {'class': 'control-label'}}) }}
    {{ form_errors(formAdd.description) }}
    {{ form_widget(formAdd.description, {'attr': {'class': 'form-control'}}) }}
  </div>


  <div class="form-edit">
    {{ form_label(formAdd.category) }}
    {{ form_errors(formAdd.category) }}
    {{ form_widget(formAdd.category, {'attr': {'class': 'form-control category_input'}}) }}
  </div>

       <hr class="divider">

  {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
  <div class="row">
    <div class="col-sm-12 button-form">
       <input type="submit" class="btn-primary btn-lg btn" value="Enregistrer" name="enregistrer" />
    </div>
</div>

  {# Génération automatique des champs pas encore écrits.
     Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
     et tous les champs cachés (type « hidden »). #}
  {{ form_rest(formAdd) }}

  {# Fermeture de la balise <form> du formulaire HTML #}
  {{ form_end(formAdd) }}


</div>
