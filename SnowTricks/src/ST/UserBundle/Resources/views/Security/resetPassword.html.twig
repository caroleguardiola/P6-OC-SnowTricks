{% extends "::layout.html.twig" %}

{% block sections_body %}

  <!-- Page Content -->
<div class="container login-page">
  <div class="row login-title">
        <div class="home_text">
            <h1 class="text-blue wow fadeInLeft" data-wow-duration="2s">Réinitialisation mot de passe</h1>
        </div>
  </div><!--End off row-->
  <div class="row page-wrapper-register">

{{ form_start(formResetPassword) }}

    <div class="register-user">
      {{ form_row(formResetPassword.plainPassword.first) }}
    </div>

    <div class="register-user">
      {{ form_row(formResetPassword.plainPassword.second) }}
    </div>

    <div class="register-submit">
      <input type="submit" class="btn-login" value="Enregistrer nouveau mot de passe" />
    </div>

  {{ form_end(formResetPassword) }}

 </div>
</div>

{% endblock %}
